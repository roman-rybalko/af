Исходящий роутинг
=================

-> - redirect_router
=> - errors_to
>> - pass_router
[] - condition
{} - unseen

* service [@services] {stop} => error
* service_defer [@services] {stop} -> service => error
* internet_verify {stop} => bounce
* hamtrap [flag_hamtrap] {continue} -> service => error
# бэкап-сообщения должны надежно доставляться - бэкап-адрес перенаправляем на mx
* backup_rcpt {continue} -> mx >> backup_end => error
* backup_domain {continue} -> mx >> backup_end => error
* backup_client {continue} -> mx >> backup_end => error
* backup_end
* spamsender [flag_spamsender] {stop} -> service => error
* internet {stop} => bounce


Транспорт
=========

spamsender
* user_hidereceived => убрать заголовки Received, исправить новый заголовок Received

mx, internet
* user_hidereceived => убрать заголовки Received, исправить новый заголовок Received
* подписать пользовательским dkim
