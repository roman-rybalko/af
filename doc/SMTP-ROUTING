Входящий роутинг

-> - redirect_router
=> - errors_to
() - defaults

* service => ? error
* mailproc (-> service) => error
* spam -> backup => error
* spamtrap (-> service) => error
* backup -> mx => error
* mx => bounce
* submission (user senders only) => error
